@{
    ViewData["Title"] = "Home Page";
}
<link rel="stylesheet" href="~/css/Home/index.css" />

@*@{
    Layout = "_LayoutHome";

}*@

<div class="container mt-5">
   <div class="text-center">
        <h1> Grupo Login </h1>
   </div>    
    @if (User.Identity.IsAuthenticated)
    {
        <div class="d-flex justify-content-center my-5">
            <button type="button" onclick="location.href='@Url.Action("ListaProductos", "Admin")'" class="btn solid">Ver producto</button>
        </div>
    }
    else
    {
    <div class="d-flex justify-content-center my-5">
        <button type="button" onclick="location.href='@Url.Action("SignIn", "Account")'" class="btn solid">Ir al login</button>
    </div>       
    }

</div>
